import { CharArrayPipe } from './char-array.pipe';

describe('CharArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CharArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
